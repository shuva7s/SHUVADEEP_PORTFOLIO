import { Loader2 } from "lucide-react";
const Loading = () => {
  return <main className="wrapper min-h-[50vh] fl_center">
    <Loader2 className="animate-spin text-primary size-8"/>
  </main>;
};

export default Loading;
